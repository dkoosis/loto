# Boot
updated: 2026-05-31

→ clean slate. main pushed (37281d0), 0 in_progress, 0 open PRs, no stray worktrees/branches.

next: pick from `bd ready` (3 P3s) when resuming impl:
- loto-qev1 [bug] flaky TestOpen_ConcurrentFirstOpen — SQLite 1802 under concurrent first-open
- loto-qnz8 Store.RotateEvents export is test-only, doc comment stale
- loto-kyib render/cli.go N+1 per-row identity I/O + dead EmitConflict wrapper

✓ done this session
- merged fix/loto-{vtg6,mcmf,5rwc} (EvalContext value object · Overlap→SameCanonical · tag/lock --help), pushed.
- /simplify: extracted render writeRestoreFailed helper. /clean: make audit green, 0 findings.
- closed loto-{vtg6,mcmf,5rwc}; pruned all worktrees + branches; gitignored .team/ heartbeats.
