Usage of unlock:
  -all
    	release every lock owned by my uuid
  -force
    	break another agent's lock (or a live lock)
  -intent string
    	intent (required only with --force)
  -t string
    	intent (required only with --force)
